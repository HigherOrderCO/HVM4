I stabilized AOT emission by resetting `clang/aot/emit.c` to the working baseline, then added focused optimizations: a head-path `USE` fast path (strict-arg force + USE-VAL inline continuation, with safe deopt on SUP/INC), contiguous APP-frame block allocation in `MAT` constructor unpacking, and non-head materialization for `OP2`/`EQL`/`AND`/`OR` to reduce unnecessary `aot_fallback_alo` deopts. I kept DUP semantics conservative (no dynamic copy-free shortcut).

Validation: test suite is stable on interpreted mode (`HVM_TEST_TIMEOUT_SECS=20 ./test/_all_.sh --interpreted-only`, all pass). AOT sweep is largely passing; one long-run transient blank output was seen once on `eql_lam_f`, but direct stress reruns passed repeatedly. On strict-let workload `test/let.hvm`, compiled-share increased from 28.57% (baseline emitter) to 42.86%, and `fallback_alo` dropped 6 -> 3.

Bench run executed as requested: `~/t/dev/bench/bench.ts --timeout 3 hvmi hvmc`. Under this strict wall clock, most entries timed out in both modes, so macro speed conclusions are inconclusive. Next: land reason-level fallback counters in emitter sites, then tackle sound DUP continuation for static-scope DUPs (not top-of-stack call DUP), and expand head reducers for boolean/equality branches while preserving net identity. Board question: prioritize semantic DUP continuation first, or a `fast_force_whnf` REF/ALO fast path first?
