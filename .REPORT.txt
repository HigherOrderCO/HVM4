Implemented foundational AOT coverage work in `clang/aot/_.c` and `clang/aot/emit.c`: DUP-aware fallback env reconstruction (`aot_fallback_alo(..., env_is_dup)`), env DUP-mask threading through emitter recursion, head/non-head `DUP` continuation, and split handling for `BJV` vs `BJ0/BJ1` so DUP binders emit `DP0/DP1` with shared cells. Added head `APP(LAM)` inline let path, direct static `REF` dispatch via emitted forward declarations, MAT stack-cap precheck, and profiling hooks for head returns and function entries.

Validation: rebuilt runtime, `./test/_all_.sh` passed fully (interpreted + AOT). Ran `~/t/dev/bench/bench.ts --timeout 3 hvmi hvmc` on final state (expected nonzero exit due timeout rows). Final table highlights: `nqueens` hvmc `1.462s` (vs prior `1.712s`, ~14.6% better), `ray_sphere` hvmc `1.566s` (vs `1.753s`, ~10.7% better), `sort_merge` hvmc `0.864s`. AOT profile samples: `nqueens` compiled_share `21.61%`, `sort_quick` `12.98%`, `sort_merge` `1.48%`, `mat_mul` `0.00%`.

Next: attack low-coverage hotspots (`mat_mul`, `sort_merge`) by reducing early head returns/fallback_alo while preserving net identity; continue avoiding MAT-SUP and top-of-stack-DUP cases. Question for board: for DUP binders during fallback, should we keep reusing live DUP cells (identity-first) or clone when env reuse risks list-link aliasing across multiple residual terms?
