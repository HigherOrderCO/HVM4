// GPU parallelism benchmark: depth 17 → 131072 leaves.
// Seq 1024 to keep total interactions ~= bench_tree (403M).

@seq = 1024

@spin = λ{
  0: 0;
  λx. @spin(x - 1)
}

@tree = λ{
  0: @spin(@seq);
  λ&x. #P{ @tree(x - 1), @tree(x - 1) }
}

@main = @tree(17)
