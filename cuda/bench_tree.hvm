// GPU parallelism benchmark: binary tree with sequential leaf work.
// Depth 16 → 65536 leaves, each spinning @seq steps.

@seq = 2048

@spin = λ{
  0: 0;
  λx. @spin(x - 1)
}

@tree = λ{
  0: @spin(@seq);
  λ&x. #P{ @tree(x - 1), @tree(x - 1) }
}

@main = @tree(16)
