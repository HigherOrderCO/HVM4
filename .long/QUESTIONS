1) Strict mismatch contract after scrutinee forcing in compiled head MAT/SWI (APP-frame ownership)

Context:
- Dominant deopt in AOT remains strict mismatch (`EXPR_UNSUPPORTED_TAG`).
- I added strict-site metadata and can now pinpoint hotspots, e.g. in `gab_takl`:
  - `site[24] kind=HEAD_MAT_NON_CTR loc=45 expect_ctr_ext=10`
- We previously tried strict forcing experiments and hit regressions (segfaults, wrong outputs, hangs), likely due to violating frame/state invariants.
- The critical ambiguity is mismatch/default continuation semantics once we force scrutinee to WHNF for tag inspection.

Precise question:
- In compiled head `MAT/SWI`, if we force scrutinee before checking tag and it mismatches the strict case, what exact value must be propagated in continuation semantics?
  1) Always forced WHNF scrutinee,
  2) Always original thunk from APP frame,
  3) Conditional rule (please specify exact condition).

Why this matters:
- This rule determines whether compiled mismatch path must rewrite the current APP frame argument slot (or equivalent) before continuing/deopting.
- Without this contract, any renewed strict forcing is unsafe and we will likely reintroduce prior correctness bugs.

Request:
- Please provide one concrete rule and a minimal interaction-style example (before/after term shape) that disambiguates the required behavior.
