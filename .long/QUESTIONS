1) Next-step boundary for DUP hot-path compilation

Context:
- This round switched AOT to live DUP binder cells (`eN`) and removed the old static-only DUP gate.
- Semantics are now preserved through deopt: pending vs substituted DUP state survives into `aot_fallback_alo(...)`.
- Full tests pass, including many DUP/autodup regressions.
- But performance is still limited because strict contexts often materialize `DP0/DP1` terms and rely on regular `wnf` to execute DUP interactions later.

Question:
- For the next step, do you want me to compile forced DP interactions directly in emitted C (mirroring `wnf` DP-frame dispatch over whnf tag: DUP-NAM/LAM/SUP/NOD), or is it acceptable to keep using runtime `wnf` for that boundary while we expand coverage elsewhere first?

Why this matters:
- Direct DP interaction compilation is the likely path to major speedups on DUP-heavy case trees, but it is the highest semantic-risk area.
- I can either prioritize correctness-conservative incremental coverage (current style) or commit to the larger DP interaction inlining pass now.
