1) Semantics-safe shortcut for `DP` scrutinees in strict `MAT/SWI`

Context:
- After direct self-call lowering, some benches are much faster but still heap-dominated by duplication:
  - `gab_takl`: `WNF_DUP_NOD` is still dominant (`231,193,148` nodes in prior heap-kind runs).
  - `trie_bitwise`: `WNF_DUP_NOD` is still dominant (`642,614,910` nodes).
- Current strict forcing (`aot_force_strict` -> `aot_force_dup`) fully materializes DUP interactions to expose scrutinee tags for compiled `MAT/SWI`.
- This is semantically conservative but very expensive.

Question:
- For a `DP0/DP1` scrutinee in strict `MAT/SWI`, is there a semantics-safe criterion to inspect/route on the dup payload tag without fully running `DUP-NOD` first?
- If yes, what exact invariants must hold (linearity, side effects, SUB bit state, payload tag class) to keep IC behavior unchanged?

2) Contract for emitting direct compiled calls across defs (not just self)

Context:
- Round 26 safely lowered demanded self-recursive `APP(REF_self, ...)` to direct `F_<self>(...)` calls, keeping depth/heap guards and preserving leftover-arg reapply.
- This reduced runtime significantly on recursion-heavy canaries while keeping heap and deopt profiles stable.
- Next obvious extension is direct calls to other known compiled refs (cross-def), instead of `aot_call_ref` table dispatch.

Question:
- Is there any runtime invariant that forbids direct `F_<callee>(...)` calls across defs when we preserve the same depth/heap guards and `a_pos` semantics?
- Or is it valid to generalize this as long as symbol/prototype ordering is handled and fallback behavior (`term_new_ref` on depth cap + arg reapply) is unchanged?
