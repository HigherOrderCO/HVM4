1) Strict-force REF policy: what is the canonical bounded compiled-call contract?

Context:
- Round 8 introduced strict local WHNF forcing (`aot_force_whnf_local`) for strict heads.
- Unbounded compiled REF re-entry caused stack overflow on `test/match_recursive_arg`.
- Current fix disables `aot_try_call` during local forcing (`AOT_TRY_CALL_ENABLED=0` in force mode), which is safe and passing.
- Round 9 kept that safety gate and improved strict OP2 forcing, but force-local REF is still interpreter-only.

Precise question:
- In strict local forcing mode, should compiled REF calls be:
  1) always disabled (current),
  2) allowed under explicit depth/fuel bounds,
  3) handled by a different trampoline mechanism?
- Please specify the exact intended invariant and one concrete pseudo-flow for recursive case-trees (`mat_self`-like) so we can implement it without C-stack blowups.

2) Telemetry contract: should expected expression captures count as deopts?

Context:
- Round 9 collapsed `gab_tak`/`gab_takl` deopts from 113/519 to 2/2.
- Remaining hits are `EXPR_APP_ARG_CAPTURE`, which correspond to intentional lazy capture of APP arguments via ALO in expression mode.
- These are not necessarily runtime mismatches; they are often planned residualization steps.

Precise question:
- Should planned captures/residualization (like APP argument capture) remain in deopt counters, or be tracked in a separate residualization metric?
- If separate, what categories are most useful for guiding compiler redesign (e.g., APP arg capture, OP2 delayed rhs, generic expression ALO, etc.)?
