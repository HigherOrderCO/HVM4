1) Strict MAT/SWI on SUP in compiled mode: intended boundary or missing interaction?

Context:
- After this round, `HEAD_DUP_ENV_CAP` is gone (`AOT_ENV_CAP=64`), and OP2 residual flood is gone (expression OP2 is structural again).
- Remaining hard deopts on key programs are almost entirely strict MAT mismatch:
  - `mat_mul`: `AOT_DEOPT=4,521,088`, top kind `HEAD_MAT_NON_CTR`
  - `ray_sphere`: `AOT_DEOPT=1,009,200`, top kind `HEAD_MAT_NON_CTR`
- In both cases, compiled head MAT does `aot_force_strict` before deopting. So these are terms that still WHNF to non-constructor at that point.

Question:
- In HVM semantics, when strict MAT/SWI scrutinee WHNFs to `SUP` (or another non-CTR/NUM topology), should compiled mode implement distribution/interaction (e.g. MAT-over-SUP / SWI-over-SUP) and stay compiled, or is deopt-to-ALO the intended semantic boundary?
- If distribution is the intended path, which minimal interaction set should be prioritized first to unblock these workloads?

2) `EXPR_APP_ARG_CAPTURE` dominates residuals: is structural arg emission valid here?

Context:
- Hard deopts dropped, but residuals are now dominated by `EXPR_APP_ARG_CAPTURE`:
  - `ray_sphere`: `AOT_RESID=11,103,520` (mostly app-arg captures)
  - `nqueens`: `AOT_RESID=7,144,921` (all app-arg captures)
- Current expression APP lowering for `fun_tag != LAM` is:
  - emit function structurally (`aot_emit_node(..., head=0)`)
  - capture arg lazily via ALO
  - build `term_new_app(fun,arg)`

Question:
- Is it semantically safe (and desirable) to recursively emit the APP argument structurally in this branch (`aot_emit_node(arg, head=0)`) instead of ALO-capturing it, relying on expression-mode laziness to preserve behavior?
- Are there invariants around sharing/substitution/sub-bit usage that require ALO capture for app arguments in this case?
