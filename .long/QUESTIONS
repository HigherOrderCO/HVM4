1) Strict MAT/SWI forcing contract: when should compiled code stop reducing and deopt?

Context:
- Current strict paths (`SWI`/`MAT` heads) call `aot_force_strict`, which does bounded DP forcing and a local `wnf` only for `APP/ALO/REF` tags.
- We still see large true-deopt counts at strict mismatch sites (`HEAD_MAT_NON_CTR` / `HEAD_SWI_NON_NUM`), which suggests forcing often stops before reaching the eventual constructor/number WHNF.
- Expert guidance says this is likely under-forcing and we should keep reducing like the interpreter.

Precise question:
- In no step-limit mode (`STEPS_ITRS_LIM==0`), should strict compiled paths keep reducing indefinitely (interpreter-equivalent behavior) until reaching `CTR/NUM`, with no deopt on mismatch?
- Or do you want a bounded safety policy in compiled mode (fuel/depth) that intentionally deopts after a threshold?
- If bounded is preferred, what is the intended observable contract (same result but possible performance cliff, or explicit divergence-preservation requirements)?

2) Expression-mode OP2 support: should compiled expression emission preserve laziness only, or partially evaluate strictness?

Context:
- New deopt metadata shows top `EXPR_UNSUPPORTED_TAG` sites in `mat_mul`, `ray_sphere`, and `nqueens` are mostly `expr_tag=OP2` (various op exts).
- Head mode already has strict OP2 handling, but expression mode currently falls back for OP2 tags.
- In expression context, we can either:
  A) build residual `term_new_op2(lhs_alo, rhs_alo)` lazily (no forcing now), or
  B) force lhs (and maybe rhs when lhs is NUM) eagerly in emitted expression code for better specialization.

Precise question:
- For AOT expression mode, what semantic target do you prefer for OP2:
  - always lazy residual construction (closer to generic expression semantics), or
  - strict partial evaluation in emitted code when safe?
- If strict partial evaluation is desired, which strictness level is acceptable in expression mode without risking observable order/duplication issues?

3) Case-tree fast path target: should we prioritize head-mode LET/APP-of-LAM next, or strict forcing/OP2 first?

Context:
- Goal text highlights LET-like forms (`(λname.body)(arg)`) and DUP/LET interleavings as major reasons whole bodies fall back to ALO.
- Current telemetry after recent fixes still shows dominant true-deopts in strict mismatch + OP2 unsupported categories.
- Implementing LET/APP-of-LAM in head mode may unlock larger portions of case-tree spines, but strict/OP2 work has immediate hotspot evidence.

Precise question:
- What ordering do you want for next implementation priority:
  1. strict `MAT/SWI` continuation + expression `OP2` coverage,
  2. head-mode LET/APP-of-LAM compilation,
  3. head-mode DUP env-cap redesign,
  or another order?
- A concrete priority order would help avoid local optimizations that don’t move admission benchmarks.
