1) Strict MAT/SWI mismatch semantics with forced scrutinee (APP-frame consistency)

Context:
- Current dominant deopt remains head strict-match mismatch (`EXPR_UNSUPPORTED_TAG`) on `gab_tak`/`gab_takl`.
- I tried forcing strict scrutinees in compiled `MAT/SWI` before tag checks.
- This exposed a subtle invariant: mismatch/default branches reuse the existing APP frame (they do not pop it), so after forcing `arg` there is a question whether the frame must hold the forced WHNF value or the original thunk.
- In interpreter terms, `APP-MAT-CTR-MIS` / `APP-MAT-NUM-MIS` produce `(default_branch forced_scrutinee_whnf)`.
- In compiled code, using original thunk vs forced WHNF in that frame appears to affect correctness and may be tied to prior regressions.

Question:
- For compiled strict `MAT/SWI`, after we force a scrutinee to WHNF for tag inspection, what is the required semantic contract for mismatch/default continuation?
  1) always pass forced WHNF scrutinee,
  2) always preserve original thunk,
  3) conditional rule (if so, exact condition).
- Please answer with one concrete rule + one minimal pseudo-example so I can codify this invariant and avoid further unsafe forcing attempts.

Why this matters:
- This is currently the blocker for implementing strict-site forcing safely; without this rule we keep hitting hard-to-debug graph corruption/regressions.
