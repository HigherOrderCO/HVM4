1) Expression-position direct compiled call wrapper semantics (REF APP chains)

Context:
- Round 19 telemetry now shows `AOT_HEAP` almost equals total `Heap` on key canaries (for `edit_dist`, `AOT_HEAP ~377.58M` vs `Heap ~377.58M`), with size-2 allocations dominating.
- This indicates we are still materializing many APP-like intermediate nodes inside compiled execution, even when `deopt=0` and `resid=0`.
- Next planned optimization is: in expression mode, when we statically see application of a `REF` head (possibly with an app spine), call compiled code directly with raw args (worker-style) instead of building `term_new_app(...)` chains.

Question:
- For correctness, is this wrapper strategy valid: call compiled worker with local args; if worker cannot fully consume (missing compiled body/depth cap/fallback), rebuild exactly the residual head+remaining args term before returning?
- Are there known interaction-net sharing pitfalls where this changes behavior versus building APP nodes first (especially with DUP-bound values that may later force)?

2) Priority between direct REF-call lowering vs constructor registerization

Context:
- New attribution shows heavy compiled-context allocation in small node sizes, but not which syntactic source dominates.
- Two candidate reductions are ready:
  A) direct expression REF-call/app-spine lowering;
  B) delayed constructor field materialization (registerize `C01..C16` fields until escape boundary).

Question:
- Given typical HVM case-tree benchmarks (`edit_dist`, `mat_mul`, `nqueens`), which should be prioritized first for maximal speedup per engineering effort?
- If there is a known ordering dependency (one unlocks/simplifies the other semantically), please point that out.
