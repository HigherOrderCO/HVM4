1) What is the strongest *safe* static predicate for ALO elision (or substitution by structural instantiation) in this runtime?

Context:
- We previously tried replacing `aot_fallback_alo(loc, 0, 0, ...)` with `heap_read(loc)` for `dep==0`; this was unsound (`autodup_bound_vars` + collapse regressions).
- We now understand ALO is also an instantiation/freshening barrier, not just env packaging.
- Residual tails are small but persistent (`EXPR_APP_ARG_CAPTURE`, `EXPR_CTR_FIELD_CAPTURE`), and we only want to elide capture where provably equivalent.

Question:
- Which exact term-shape constraints are sufficient to guarantee semantic equivalence without ALO?
- Is there an accepted conservative subset (for example, fully frozen data subset) we should target first?

2) Direct compiled REF calls without APP frame allocation: which invariants must be preserved at AOT/interpreter boundaries?

Context:
- Deopts are near-zero on canaries; performance now appears heap-allocation bound.
- A likely dominant cost is APP frame materialization (`heap_alloc(2)` + stack push/pop) even on fully compiled call chains.
- We want to pass known-arity args via an AOT calling convention and reify APP nodes only when escaping to generic runtime/deopt.

Question:
- Which runtime behaviors strictly depend on those APP nodes being materialized *eagerly*?
- For strict interactions (`SWI/MAT` forcing, DUP/SUP edges), what minimal boundary rule avoids semantic drift?

3) Trampoline design choice for non-tail / mutual recursion in compiled mode

Context:
- Round 15 added self-tail loop lowering in emitted functions; this removed `gab_deriv` `CALL_DEPTH_CAP` deopts (`97 -> 0`) while preserving tests/parity.
- Remaining recursion depth risk now comes from mutual/non-tail recursive compiled calls via `aot_call_ref`.

Question:
- Preferred design: (A) explicit `CALL(fn_id,args...)` trampoline return protocol, or (B) shared work loop with mutable "current callee" state?
- Are there existing invariants around stack/base ownership that make one approach clearly safer in HVM’s runtime architecture?
