1) Exact semantic contract for compiled `DP` shortcutting in strict `MAT/SWI`

Context:
- Current AOT strict forcing path for scrutinees (`aot_force_strict` -> `aot_force_dup`) is conservative: for `DP0/DP1`, it may run generic DUP interactions (`wnf_dup_nod`) until a concrete WHNF appears.
- This is correct but very expensive on key benchmarks (`gab_takl`, `trie_bitwise`), where heap attribution has shown `WNF_DUP_NOD` dominating total nodes.
- In compiled case-tree code, many strict `MAT/SWI` checks only need the scrutinee tag and possibly constructor fields, not a fully materialized duplicated structure.

Question:
- For a strict scrutinee known to be `DP0/DP1`, what is the strongest semantics-safe shortcut we can apply before full `wnf_dup_nod` materialization?
- Specifically, can compiled code branch directly on DUP payload tag classes (NUM / CTR / others) and/or synthesize projected `DP0(field_i)` / `DP1(field_i)` bindings lazily, while preserving exact IC behavior?
- Please state required invariants explicitly:
  - linearity / consumption guarantees,
  - when the DUP cell must exist physically vs can stay virtual,
  - SUB-bit obligations,
  - cases that must still fallback to generic interpreter interaction.
