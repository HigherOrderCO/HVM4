1) Allocation attribution boundary after Round 17 (missing heap still large)

Context:
- Round 17 removed compiled APP-frame and env-bind-list materialization from hot paths.
- On canaries, `AOT_ALLOC` dropped drastically and is now often dominated by `DUP_CELL`.
- But for `edit_dist`, gap remains huge even with `AOT_DEOPT=0` and `AOT_RESID=0`:
  - `AOT_ALLOC total_nodes=51,264`
  - runtime `Heap=377,583,005`
- So most allocations are still outside our currently attributed explicit AOT sites.

Question:
- In HVM runtime, where should we instrument next for highest signal-to-noise to explain this gap?
- Should we prefer a thread-local compiled-context counter around global `heap_alloc`, or per-helper instrumentation first (`aot_force_*`, `wnf_*` interactions reached from compiled strict forcing)?

2) DUP_CELL elimination opportunities with current semantics

Context:
- After removing APP/env bind materialization, `DUP_CELL` became the dominant explicit AOT allocation class:
  - `mat_mul`: 18,128,240 `DUP_CELL` calls
  - `ray_sphere`: 13,458,320
  - `nqueens`: 19,354,763
- Current emitter allocates a DUP cell for each compiled DUP binder to preserve DP0/DP1 behavior.

Question:
- Which DUP patterns are semantically safe to scalarize/avoid cell allocation in compiled mode?
- Is there a known criterion (linearity / copy-free payload / no escaping DP references / no SUP interaction) to prove a DUP cell can stay in registers and only materialize on fallback?

3) Next recursion step: SCC trampoline with raw arg ABI

Context:
- Self-tail `REF` loop is already in place and stable.
- We now pass raw local args across compiled calls (`a_args/a_pos`) and only reify at interpreter boundary.
- Non-self recursion still uses C recursion via `aot_call_ref`.

Question:
- For HVM semantics and current stack ownership (`stack,s_pos,base`), is SCC-local dispatch loop the preferred next step over a generic trampoline return-protocol?
- Any pitfalls when combining SCC dispatch with pending raw local args that are not yet reified into runtime APP frames?
